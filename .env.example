ANTHROPIC_API_KEY=...         # required if using Claude models
COMPOSIO_API_KEY=...          # required if using Composio to access GMail
OPENAI_API_KEY=...            # required if using GPT models
TAVILY_API_KEY=...            # required (web search)
COMPOSIO_API_KEY=...          # required (Gmail delivery via Composio — get key at app.composio.dev)
LOG_LEVEL=INFO
